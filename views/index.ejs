<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product List</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f8f9fa;
            margin: 0;
            padding: 20px;
        }

        .header,
        .container {
            width: 80%;
            margin: 0 auto;
            padding: 20px;
            background-color: #ffffff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            border-radius: 8px;
            margin-bottom: 20px;
        }

        .header a {
            margin-right: 20px;
            color: #007bff;
            text-decoration: none;
            font-size: 18px;
        }

        .header a:hover {
            text-decoration: underline;
        }

        .header div {
            font-size: 20px;
            margin-bottom: 20px;
        }

        .product {
            border-bottom: 1px solid #ddd;
            padding: 20px 0;
            display: flex;
            align-items: center;
        }

        .product img {
            margin-right: 20px;
            border-radius: 8px;
        }

        .product-details {
            flex-grow: 1;
        }

        .product-details h2 {
            margin: 0 0 10px 0;
            font-size: 24px;
            color: #333;
        }

        .product-details p {
            margin: 5px 0;
            color: #555;
        }

        .product-details .price {
            font-size: 18px;
            font-weight: bold;
            color: #000;
        }

        .product-details .add-to-cart {
            display: inline-block;
            margin-top: 10px;
            padding: 10px 20px;
            background-color: #28a745;
            color: #fff;
            text-decoration: none;
            border-radius: 5px;
            transition: background-color 0.3s;
        }

        .product-details .add-to-cart:hover {
            background-color: #218838;
        }
    </style>
</head>

<body>
    <div class="header">
        <% if (username) { %>
            <div>Welcome, <%= username %>
            </div>
            <% } %>
                <% if (!login) { %>
                    <a href="http://localhost:6789/autentificare">Log In</a>
                    <a href="http://localhost:6789/signUp">Sign Up</a>
                    <% } else { %>
                        <a href="http://localhost:6789/shopping-cart">Cart</a>
                        <% } %>
    </div>

    <% if (dbLoad) { %>
        <div class="container">
            <% products.forEach((product)=> { %>
                <div class="product">
                    <img src="<%= product.productId %>.jpg" width="400" height="300" alt="<%= product.name %>">
                    <div class="product-details">
                        <h2>
                            <%= product.name %>
                        </h2>
                        <p>Description: <%= product.description %>
                        </p>
                        <p class="price">Price: <%= product.price %>
                        </p>
                        <% if (login) { %>
                            <a href="/add-to-cart?id=<%= product.productId %>" class="add-to-cart">Add to Cart</a>
                            <% } %>
                    </div>
                </div>
                <% }); %>
        </div>
        <% } %>
</body>

</html>